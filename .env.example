# 环境变量配置（在 Vercel 项目 Settings → Environment Variables 中设置）

# Neon Postgres 连接字符串（数据库）
DATABASE_URL=postgresql://user:password@hostname/dbname?sslmode=require

# 企业微信身份认证配置
# 获取方式：企业微信管理后台 → 应用管理 → 自建应用
WEWORK_CORP_ID=your-corp-id
WEWORK_AGENT_ID=1000002
WEWORK_SECRET=your-app-secret
